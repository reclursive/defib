const Saved = () => {
    return (
        <div>
            <h1>
                Saved
            </h1>
        </div>
    )
}

export default Saved

//protected route
//saved creates object from api, segments elements, allow crud for notes that attach to
//any element. Submitted to production will link note to the original object that appears 
//in public infinite scroll